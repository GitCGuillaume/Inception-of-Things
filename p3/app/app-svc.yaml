apiVersion: v1
kind: Service
metadata:
  name: development
  namespace: argocd
spec:
  selector:
    app.kubernetes.io/name: development
  ports:
    - protocol: TCP
      name: http
      port: 8888
      targetPort: 8888